<nav id="header" class="navbar sticky-top p-3">
  <div class="container-fluid">
    <a href="./index.html" class="navbar-brand h1">座標の地図表示と座標・緯度経度の変換</a>
    <% if (page == 'toppage') { %>
    <div class="d-md-flex align-items-md-stretch">
      <div id="openHelp" class="me-2">
        <div class="d-print-none">
          <button data-bs-toggle="offcanvas" data-bs-target="#offcanvasScrolling" aria-controls="offcanvasScrolling"
            class="button button--dialog-open">
            <img src="./assets/help.svg" height="24">使い方を表示</button>
        </div>
      </div>
      <div id="togglePreview">
        <div class="p-2">
          <label class="switch__label d-flex align-items-center mb-0" for="printPreviewBtn">
            <div class="form-check form-switch ms-1">
              <input id="printPreviewBtn" class="form-check-input" type="checkbox" role="switch">
            </div>
            <p id="printPreviewBtnTitle" class="form-check-label text-white fs-6 ms-2 me-3 mb-0">印刷時と同じ見た目にする</p>
          </label>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</nav>
